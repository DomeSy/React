Redux遵循的三个原则
1>单一事实来源：
a.整个应用的状态存储在单个 store 中的对象/状态树里。
b.单一状态树可以更容易地跟踪随时间的变化，并调试或检查应用程序。
2>状态是只读的：
a.改变状态的唯一方法是去触发一个动作。
b.动作是描述变化的普通 JS 对象。
c.就像 state 是数据的最小表示一样，该操作是对数据更改的最小表示。
3>使用纯函数进行更改：
a.为了指定状态树如何通过操作进行转换，你需要纯函数。
b.纯函数是那些返回值仅取决于其参数值的函数。

